import React from 'react';
import { LogIn } from './LogIn';

// Take that login component and use it.
export const LogInPage = () => {
    return (
        <div>
        <LogIn/>
        </div>
    );
};