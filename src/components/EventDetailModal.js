import React from 'react'

// window with event details
// options:
// 1. pass event item with all information into the component
// export const EventDetailModal = ({event: event}) => {
// 2. pass each detail into the component
// export const EventDetailModal = ({
//   eventId: eventId,
//   detail:detail
// }) => {
export const EventDetailModal = ({ item:event }) => {

  // check following files in Luke's code
  // 1. EventListPage: focus on Modal and EventDetailForm in return
  // 2. EventDetailForm: this can be the base of the form
  // 3. Modal: when time allows - for looking good purpose

  return (
    <div>
      
    </div>
  )
}

export default EventDetailModal;